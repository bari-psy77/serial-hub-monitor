# CHANGELOG

이 파일은 1.4.0 에서 신설했다. 그 이전 내역은 커밋 제목에서 소급해 정리한 것이다.

## 1.4.0 — 2026-08-14

### 추가

- **로그 저장 위치 옵션**: 날짜별(MMDD) 하위 폴더 사용 여부를 [설정 → 로그] 에서 고른다.
  **기본은 로그 폴더에 바로 저장**(이전 동작은 항상 날짜 폴더였다).
- **덮어쓰기 확인**: 기록 시작 시 같은 이름의 파일이 있으면 **덮어쓰기 / 이어쓰기 / 취소**
  를 묻는다. 기본 버튼은 안전한 이어쓰기이고, 자동화(브리지·`ask=False`) 경로는 묻지
  않고 이어 쓴다.
- **로그 뷰어**: [파일 → 로그 파일 열기(뷰어)…] 로 지난 로그를 불러와 분석한다.
  포트별·병합 파일은 타임스탬프와 출처를 복원하고, 모르는 형식도 원문 그대로 열린다.
  검색(부분일치/정규식/대소문자)·하이라이트·결과 저장은 필터드뷰와 같고, 여러 파일을
  한 번에 열면 시간순으로 병합돼 소스별 체크박스로 골라 본다.
- **내장 터미널**: [보기 → 터미널 열기] 또는 액션 바의 [🖥 터미널] 로 ConPTY 기반 실제
  PowerShell 을 하단 도크에서 쓴다 (색·커서 이동·화면 지움·TUI 지원). 마우스 휠과
  오른쪽 스크롤바로 스크롤백을 오가고, 타이핑하면 맨 아래로 돌아온다. 관리자 셸은 UAC
  경계 때문에 임베드할 수 없어 [관리자 PowerShell (외부 창)] 이 승격 창을 띄운다.
- **도크 배치**: 뷰어·터미널 도크는 하단에서 **탭으로 묶이고**, 제목줄을 끌면 독립 창이
  된다. 닫으면 자리를 즉시 중앙 화면에 돌려준다.

### 고침

- 터미널의 전각(한글) 문자가 글자마다 벌어지고 색 경계에서 줄이 어긋나던 문제 —
  pyte 의 wide-char 연속 셀을 칸 단위로 처리하고, 글자를 격자 칸에 배치하며, 테마
  QSS 가 덮어쓰던 터미널 폰트를 고정했다.
- 터미널 재시작이 이전 화면 한가운데에 새 프롬프트를 그리던 문제 — 재시작 시 화면과
  스크롤백을 비운다.
- `uitest.py` 가 1.3.0 의 기본 언어 변경(영어) 이후 한국어 단언과 어긋나 깨져 있던 것.
- `make_docs.py` 의 ruff E501 5건.
- 소스로 실행할 때 저장소에 생기던 `settings.json` 을 gitignore 에 추가.

### 의존성

- `pyte>=0.8`, `pywinpty>=2.0` (Windows 전용 마커) 추가 — 내장 터미널용.

### 문서

- 사용설명서(한/영)에 로그 뷰어·내장 터미널 절 추가, 저장 위치 설명을 새 동작에 맞춤.
- README 2종 갱신.

## 1.3.0 — 2026-08-11

- 한국어/영어 2개 언어 지원, **기본 표시 언어를 영어로 변경**. 번역표는
  `core/i18n_en.py` 한 곳이고, 누락·자리표시자 불일치를 selftest 가 강제한다.
- 사용설명서 HTML 을 한/영 2종으로 제공하고 F1 도움말이 언어 설정을 따라간다.
- README 를 영어·한국어로 분리.
- GitHub Actions CI 도입 (`selftest.py` 의 두 절반을 ubuntu + offscreen Qt 로 실행).

## 1.2.1 이전

`git log` 참조 — 이 저장소는 1.2.1 임포트(`8f07a9b`)부터 이력을 갖는다.
